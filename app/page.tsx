import { HomeContent } from "@/components/home-content"

export default function Page() {

    return (
        <main className="flex flex-col min-h-[calc(100vh-64px-80px)]">
            <HomeContent/>
        </main>
    );
}